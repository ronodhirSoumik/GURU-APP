<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.myapplication.app"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".registerTutor" />
        <activity android:name=".UserProfile" />
        <activity android:name=".firstGuardian" />
        <activity android:name=".userRegistration" />
        <activity
            android:name=".tutorProfileSecond"
            android:label="@string/title_activity_tutor_profile_second"
            android:theme="@style/AppTheme" />
        <activity android:name=".checkTutorProfile" />
        <activity
            android:name=".ScrollingActivityforTutor"
            android:label="@string/title_activity_scrolling_activityfor_tutor"
            android:theme="@style/AppTheme" />
        <activity android:name=".fragmentActivity" />

        <service android:name=".backgroundNotification" />

        <activity android:name=".offerToDetails" />
        <activity android:name=".setTuitionDetails"></activity>
    </application>

</manifest>